---
description: Godot 项目使用 GDScript 开发
globs: "**/*.gd","**/*.tscn"
alwaysApply: true
---

# Godot 语言规范

## 1. 使用 GDScript

当项目为 Godot 游戏（存在 `project.godot`）时，**所有脚本必须使用 GDScript**（`.gd` 文件），不使用 C# 或其他语言。

## 2. 约定

- 脚本扩展名：`.gd`
- 场景扩展名：`.tscn`
- 资源扩展名：`.tres`、`.res`
