---
description: UI 布局规范；同级别元素优先横向排布；画布占满全屏；设置菜单入口（主菜单+暂停菜单）；按键绑定可修改
alwaysApply: true
---

# UI 布局规范

涉及 UI 设计时遵循以下原则。

## 1. 同级别元素优先横向排布

同级 UI 元素（如按钮组、选项、面板）优先采用横向（水平）排列，再考虑纵向或网格。

## 2. 画布占满全屏

- 画布在不与其它元素冲突的前提下，应占据全屏
- 使用锚点/边距使画布铺满可用区域

## 3. 多画布时的全屏划分

当同级别存在多个画布时：

- 按需将全屏空间划分给各画布
- **优先横向划分**（左右分屏），其次纵向划分（上下分屏）
- 示例：左右两栏、左中右三栏等

## 4. 设置菜单与按键绑定

- **按键绑定**：可在设置菜单中修改（移动、暂停、血条切换等）
- **设置入口**：调出设置菜单的按钮需同时存在于：
  - 主菜单
  - 游戏暂停菜单
